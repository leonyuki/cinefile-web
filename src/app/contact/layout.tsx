export const metadata = {
  title: 'Contact | CinéFile',
  description: 'CinéFileへの協賛・お問い合わせはこちらから。',
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
