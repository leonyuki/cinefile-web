# CinéFile 管理画面マニュアル

CinéFileウェブサイトの管理画面（`/admin`）の使い方マニュアルです。ニュースやメンバー情報の更新など、日常的な運用を行う方向けの内容です。

コードやサーバーに関する内容は含みません（開発者向けの情報は [README.md](./README.md) を参照してください）。

---

## 1. ログイン方法

1. `https://cinefile.jp/admin/login` にアクセスします。
2. 発行された「ユーザーネーム」と「パスワード」を入力してログインします。
3. ログインに成功すると自動的に管理画面（`/admin`）に移動します。

アカウントは自分で新規作成することはできません。アカウントが必要な場合はADMIN権限を持つメンバーに発行を依頼してください（→ 6章）。

セッション（ログイン状態）は24時間で切れます。再度ログインし直してください。

## 2. 権限（ADMIN / PR / USER）について

管理画面でできることはアカウントの「権限」によって決まります。

| 権限 | 表示されるタブ | できること |
|---|---|---|
| **ADMIN** | NEWS / BLOG / PEOPLE / PARTNERS / USERS | すべての操作。アカウントの発行・削除・権限変更も可能 |
| **PR** | NEWS / BLOG / PARTNERS | お知らせ・ブログ・パートナー情報の投稿・編集・削除 |
| **USER** | PEOPLE | 自分自身のプロフィール（メンバー紹介）の編集のみ |

自分の権限は画面左上、タイトルの横に小さく表示されています（例: `ADMIN`）。

タブが足りない・操作できないボタンがある場合は、権限が足りていない可能性があります。ADMINのメンバーに確認してください。

> 補足: USER権限でログインした直後は、タブのボタンには出てこない「自分のアカウント情報」画面が最初に表示されます。プロフィールを編集したい場合は上部の **PEOPLE** タブを押してください。

## 3. NEWS・BLOGタブ（お知らせ・ブログ）

対象: **ADMIN / PR**

### 一覧画面
- 投稿済みの記事が新しい順に並びます。
- 「編集」で内容を修正、「削除」で記事を完全に削除できます（削除は取り消せません）。

### 新規作成・編集
1. 「新規作成」ボタンを押します。
2. 以下を入力します。
   - **タイトル**（必須）
   - **カテゴリ**（任意、例:「お知らせ」）
   - **記事の抜粋・概要**（一覧画面に表示される1行紹介文）
   - **本文**（必須）
   - **メイン画像**（BLOGを新規作成する場合のみ表示・必須。NEWSの投稿や、既存記事の編集時には表示されません）
3. 「入力内容を確認する」を押すと確認画面になります。内容を確認し、問題なければ「この内容で送信する」を押して確定します。修正したい場合は「修正する」で入力画面に戻れます。

公開された記事はサイトの `/media` ページと個別ページに反映されます（反映まで最大60秒程度かかることがあります）。

## 4. PEOPLEタブ（メンバープロフィール）

対象: **全員**（ただしUSER権限は自分自身の情報のみ編集可能）

1. 「編集するメンバー」から自分の名前を選びます（ADMINは全員から選択可能）。
2. 以下を入力・変更できます。
   - 名前（日本語表記・英語表記）
   - 役職（POSITION）
   - プロフィール画像（アップロードすると差し替わります）
   - ABOUT用の自己紹介文
   - SNS・Webリンク（Instagram / X / Facebook / YouTube / GitHub / LinkedIn / note / Website / その他）
   - 参加・担当したイベント（複数選択可）
   - Markdown形式のポートフォリオ本文（`/people/[名前]` ページに表示される詳細プロフィール）
3. 「確認画面へ進む」→ 内容を確認 →「この内容で送信する」で反映されます。

⚠️ **他のメンバーのプロフィールは編集できません**（ADMIN権限を除く）。誤って他人の名前を選んでも、保存時にサーバー側で拒否されます。

## 5. PARTNERSタブ（協賛・パートナー企業）

対象: **ADMIN / PR**

- 企業・団体名、Webサイト URL（任意）、ロゴ画像（必須）を入力して登録します。
- このタブには一覧・編集・削除機能はありません。登録専用です。修正や削除が必要な場合はADMINに相談してください。

## 6. USERSタブ（アカウント管理）

### ADMIN権限の場合
- 「新しい運営・閲覧メンバーの直接登録」フォームから、ユーザーネーム・初期パスワード・権限（ADMIN / PR / USER）を指定して新規アカウントを作成できます。
- 一覧から既存アカウントの権限変更、削除ができます。

### PR・USER権限の場合
- 自分自身のアカウント情報のみ表示されます（変更操作はできません）。

## 7. パスワードについて

- パスワードは安全にハッシュ化して保存されており、管理者を含め誰も元のパスワードを見ることはできません。
- 万が一パスワードを忘れた場合、自分でリセットする機能は現状ありません。ADMINに依頼して、USERSタブから新しいパスワードでアカウントを作り直してもらうか、開発者にDBの修正を依頼してください。

## 8. イベントアーカイブページを更新したい場合

**アーカイブページ（過去・今後のイベント紹介）は管理画面から編集できません。** コードを直接編集し、Gitにコミット・デプロイする必要があるため、開発者（または編集できるメンバー）に依頼してください。

依頼する際は、次のどちらを直したいか伝えるとスムーズです。

- トップページや `/archive` 一覧に出ている概要（タイトル・日付・サムネイル画像など）→ `src/data/events.ts`
- 個別イベントページの本文（ステートメント・参加アーティスト紹介・クレジット等）→ `src/app/archive/《イベントのスラッグ》/data.ts`

この2つは別ファイルなので、両方に反映したい変更は両方の編集が必要です。

## 9. よくあるトラブル

| 症状 | 原因・対処 |
|---|---|
| ログインできない | ユーザーネーム・パスワードを再確認。それでもダメならADMINにアカウント状態を確認してもらう |
| 見えるはずのタブが出ない | 権限（ADMIN/PR/USER）が足りていない可能性。ADMINに権限変更を依頼 |
| 他のメンバーの情報を編集しようとするとエラーになる | 仕様です。ADMIN以外は自分の情報のみ編集可能 |
| 画像をアップロードしたのに反映されない | 数十秒〜1分ほど時間を置いて再読み込みしてください（キャッシュの都合） |
| 投稿・削除ボタンが反応しない/押せない | 権限不足、または通信エラーの可能性。画面に表示されるエラーメッセージを確認してください |
